-- Keep the schema: golang-migrate stores schema_migrations here.
-- Dropping the schema in this step breaks version tracking on migrate down.
-- To remove the schema entirely, drop it manually after a successful migrate-down.
SELECT 1;
